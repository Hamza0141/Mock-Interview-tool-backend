// // webhook-server.js
// require("dotenv").config();
// const webhookApp = require("./webhook/webhookApp");

// const WEBHOOK_PORT = process.env.WEBHOOK_PORT;

// webhookApp.listen(WEBHOOK_PORT, () => {
//   console.log(`Stripe webhook server running on port ${WEBHOOK_PORT}`);
// });
